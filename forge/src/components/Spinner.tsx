// Matches C:\MimirDev\desktop\src\components\IncidentViewerScreen.tsx's SmallSpinner --
// same visual language across both apps.
export function Spinner() {
  return <span className="inline-block h-3 w-3 animate-spin rounded-full border-2 border-current/25 border-t-current" />
}
